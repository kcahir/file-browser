HTML5 File Browser
==================

Navigate a mocked file system hierarchy in the browser.

## Install Nodemon

Run `npm install -g nodemon`

## Install Assets

Run `npm install`

## Run Application

Run `nodemon bin/www`

